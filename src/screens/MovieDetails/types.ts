export type Details = {
  route: {
    params: {
      movie: {
        id: string;
        title: string;
        backdropPath: string;
        overview: string;
        releaseDate: string;
        voteAverage: number;
      };
    };
  };
};
